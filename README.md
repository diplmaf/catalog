# 🚗 Каталог автомобилей дилерского центра, Николаева ИС-323 

- const readline = require('readline')  
<br> подключение встроенного модуля для чтения пользовательского ввода

- const rl = readline.createInterface({ input: process.stdin, output: process.stdout })  
<br> создание интерфейса ввода/вывода для взаимодействия с консолью

- let cars = []  
<br> массив для хранения всех автомобилей в памяти

- let nextId = 1  
<br> счётчик для генерации уникальных ID новых автомобилей

- function askQuestion(question) { return new Promise((resolve) => { rl.question(question, resolve); }); }  
<br> вспомогательная функция, превращающая ввод в Promise для удобства await

- async function addCar() {  
<br> начало функции добавления нового автомобиля

- console.log('\n--- Добавить новую машину ---')  
<br> вывод заголовка операции

- const manufacturer = await askQuestion('Производитель: ')  
<br> запрос у пользователя названия производителя

- const model = await askQuestion('Модель: ')  
<br> запрос модели автомобиля

- const year = parseInt(await askQuestion('Год выпуска: '))  
<br> запрос года и преобразование строки в целое число

- const condition = await askQuestion('Состояние (новая / Б/У): ')  
<br> запрос состояния автомобиля

- const price = parseFloat(await askQuestion('Цена (руб.): '))  
<br> запрос цены и преобразование в число с плавающей точкой

- const car = { id: nextId++, manufacturer, model, year, condition, price }  
<br> создание объекта автомобиля с автоматическим ID

- cars.push(car)  
<br> добавление автомобиля в общий массив

- console.log('✅ Машина добавлена:', car)  
<br> подтверждение успешного добавления

- }  
<br> завершение функции addCar

- function listCars() {  
<br> начало функции вывода всех автомобилей

- console.log('\n=== Каталог машин ===')  
<br> заголовок списка

- if (cars.length === 0) { console.log('Каталог пуст.'); return; }  
<br> проверка на пустой каталог и ранний выход

- cars.forEach(car => { console.log(`ID: ${car.id}, Производитель: ${car.manufacturer}, Модель: ${car.model}, Год: ${car.year}, Состояние: ${car.condition}, Цена: ${car.price} руб.`); })  
<br> вывод каждой машины в читаемом формате

- }  
<br> завершение функции listCars

- function findCarById(id) { return cars.find(car => car.id === id); }  
<br> поиск автомобиля по ID, возвращает объект или undefined

- async function updateCar() {  
<br> начало функции редактирования

- console.log('\n--- Редактировать машину ---')  
<br> заголовок операции

- const id = parseInt(await askQuestion('Введите ID машины для редактирования: '))  
<br> запрос ID для поиска

- const car = findCarById(id)  
<br> попытка найти автомобиль

- if (!car) { console.log('❌ Машина с таким ID не найдена.'); return; }  
<br> обработка случая, если автомобиль не найден

- console.log('Текущие данные:', car)  
<br> вывод текущих данных для справки

- car.manufacturer = await askQuestion(`Новый производитель (${car.manufacturer}): `) || car.manufacturer  
<br> обновление производителя; если ввод пуст — сохраняется старое значение

- car.model = await askQuestion(`Новая модель (${car.model}): `) || car.model  
<br> аналогично для модели

- const yearInput = await askQuestion(`Новый год (${car.year}): `); car.year = yearInput ? parseInt(yearInput) : car.year  
<br> обновление года с безопасным преобразованием

- car.condition = await askQuestion(`Новое состояние (${car.condition}): `) || car.condition  
<br> обновление состояния

- const priceInput = await askQuestion(`Новая цена (${car.price}): `); car.price = priceInput ? parseFloat(priceInput) : car.price  
<br> обновление цены с преобразованием

- console.log('✅ Машина обновлена:', car)  
<br> подтверждение успешного редактирования

- }  
<br> завершение функции updateCar

- async function deleteCar() {  
<br> начало функции удаления

- console.log('\n--- Удалить машину ---')  
<br> заголовок операции

- const id = parseInt(await askQuestion('Введите ID машины для удаления: '))  
<br> запрос ID

- const index = cars.findIndex(car => car.id === id)  
<br> поиск индекса автомобиля в массиве

- if (index === -1) { console.log('❌ Машина с таким ID не найдена.'); return; }  
<br> обработка отсутствия автомобиля

- const deleted = cars.splice(index, 1)[0]  
<br> удаление из массива и получение удалённого объекта

- console.log('✅ Машина удалена:', deleted)  
<br> подтверждение удаления

- }  
<br> завершение функции deleteCar

- async function showMenu() {  
<br> начало главного меню

- console.log('\n--- Меню дилерского центра ---')  
<br> заголовок меню

- console.log('1. Добавить машину')  
<br> пункт меню

- console.log('2. Показать все машины')  
<br> пункт меню

- console.log('3. Редактировать машину')  
<br> пункт меню

- console.log('4. Удалить машину')  
<br> пункт меню

- console.log('5. Выйти')  
<br> пункт выхода

- const choice = await askQuestion('Выберите действие (1–5): ')  
<br> запрос выбора пользователя

- switch (choice.trim()) {  
<br> обработка выбора

- case '1': await addCar(); break  
<br> вызов добавления

- case '2': listCars(); break  
<br> вызов просмотра

- case '3': await updateCar(); break  
<br> вызов редактирования

- case '4': await deleteCar(); break  
<br> вызов удаления

- case '5': console.log('До свидания!'); rl.close(); return  
<br> завершение программы

- default: console.log('⚠️ Неверный выбор. Попробуйте снова.')  
<br> обработка неверного ввода

- }  
<br> завершение switch

- await showMenu()  
<br> рекурсивный вызов меню для продолжения работы

- }  
<br> завершение функции showMenu

- console.log('🚗 Добро пожаловать в каталог дилерского центра!')  
<br> приветствие при запуске

- showMenu().catch(err => { console.error('Ошибка:', err); rl.close(); })  
<br> запуск меню с обработкой критических ошибок

# Спасибо за внимание
